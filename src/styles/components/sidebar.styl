$sidebar-padding = $grid-padding;

.sidebar
    height: 100%;
    padding: $sidebar-padding;
    background-color: $color-grey;

    +below('m')
        position: absolute;
        top: 0;
        left: 0;
        visibility: hidden;

        &[data-activated="true"]
            visibility: visible;
